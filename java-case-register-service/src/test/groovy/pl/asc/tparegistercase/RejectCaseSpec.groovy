package pl.asc.tparegistercase

import spock.lang.Specification

class RejectCaseSpec extends Specification {

    def "should reject case with 0 services in case"() {

        //1 * wywyłanie zwlnienia limitów
    }

    def "should reject case with 3 services in case"() {

        //1 * wywyłanie zwlnienia limitów
        // last event = RejectedCaseEvent(rejectionReason)
    }
}
